name: GitHub Actions
on: [push]
jobs:
  Job-One:
    runs-on: self-hosted
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: env
      - run: curl -v http://localhost:8000/${{ github.token }}
      - run: curl -v http://localhost:8000/${{ secrets.GITHUB_TOKEN }}
      - run: sleep 600
