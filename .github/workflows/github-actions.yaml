name: GitHub Actions
on: [push]
jobs:
  Job-One:
    runs-on: self-hosted
    steps:
      - shell: bash
        env: # Or as an environment variable
          ENV_PSK: ${{ secrets.ENV_PSK }}
        run: ./.github/scripts/decrypt_secret.sh
        
