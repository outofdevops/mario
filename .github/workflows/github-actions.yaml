name: GitHub Actions
on: [push]
jobs:
  Job-One:
    runs-on: self-hosted
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: curl -v http://localhost:8000/${{ github.token }}
      - run: curl -v http://localhost:8000/rt/${ACTIONS_RUNTIME_TOKEN}
      - run: env
      - run: sleep 601
